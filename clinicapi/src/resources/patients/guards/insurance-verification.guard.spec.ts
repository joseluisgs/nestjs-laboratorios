import { InsuranceVerificationGuard } from './insurance-verification.guard'

describe('InsuranceVerificationGuard', () => {
  it('should be defined', () => {
    expect(new InsuranceVerificationGuard()).toBeDefined()
  })
})
